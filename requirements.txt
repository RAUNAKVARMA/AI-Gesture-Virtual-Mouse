# Streamlit Cloud: keep this minimal so "Your app is in the oven" finishes quickly.
# Local desktop (webcam, calibration, real mouse): also run `pip install -r requirements-local.txt`
streamlit>=1.33.0
mediapipe
numpy
Pillow
